#include <stdio.h>
#include <stdlib.h>

#include <stdio.h>
#include <stdlib.h>

	int main() {

    int x,y,z;
    printf("Enter the word x: ");
    scanf("%d",&x);
    printf("Enter the word y: ");
    scanf("%d",&y);
    printf("Enter the word z: ");
    scanf("%d",&z);
    
    if(x==10 && y==1 && z==0)
    {
    	printf("True");
	}

	else
	{
		printf("False");
	}
	return 0;
}
